# 更新履歴

新しい順。`src/deck2untap.js` の `C2U_VER` を上げたら、ここに1行足す（パネル右上に出る版番号と同じ）。

## v31
- **操作の帯をパネル下に固定**：untap で取り込んだあとの「選んだカードで取り込み直す（N 件）」「照合・登録を頼む（N 件）」を、一覧をスクロールしても押せるように下端に固定。ボタンに件数を表示
- **ヴァイス：デッキのページで「開く」を飛ばして直接「コピー」**できるボタンを追加（中身を確かめたいときは「開く」）
- **接続切れのときに「ページを再読み込みする」ボタン**
- **コピーしたあとに次の一歩を案内**：「次は untap で：デッキの画面を開いて、ブックマーク →『コピーしたデッキを取り込む』」と untap を開くリンク（全ゲーム）

## v30
- ws-names.json は登録担当のルーティンが自動でコミットするようになったので、untap 側のカード登録アシストから「英語名データを書き出す」ボタンを外した（手で上げると上書きし合うため）。手順書・README も合わせて修正

## v29
- パネルの見出し（「–」「⇆」「×」）を、中身をスクロールしても上に残るようにした

## v28
- 候補の「これを使う」を選んだことが分かるようにした（untap の画面ではラジオボタンの丸が見えないため）。選んだ候補は**青い枠と「✓ 選択中」**、カードの下に「✓ 選択中：『…』を使う」／「未選択：このカードは依頼に入ります」を表示
- 候補の枠のどこを押しても選べる（名前をコピー・画像の拡大は除く）

## v27
- 小さくしたパネルが残っているときにブックマークを押すと元に戻す動きは、**同じ版のパネルのときだけ**にした。古い版のパネルが残っていたら作り直して新しい版で開く（更新直後に古いパネルが戻ってきて、ボタンが反応しないように見えることがあったため）

- **untap との接続が切れているとき**（タブを放っておくとロゴと「Login」の読み込み画面になる）は、取り込みを始めずに「接続が切れています。ページを再読み込みしてからもう一度」と表示する。番号の照合も待たずに飛ばす（問い合わせの待ち時間も 10 秒 → 6 秒に）

## v26
- 小さくしたときの位置を変更：右に寄せているときは**右上**、左に寄せているときは**左上**に、見出しの帯だけ残す（下の「うまく動かないとき」も隠す）。ボタンを画面の端側に並べ、「–」と「▢」が同じ位置に来るようにした
- 小さくした帯は、タイトルをつかんで**上下にドラッグ**できる。位置と左右は、そのサイトのブラウザに覚えて次回も使う

## v25
- パネル右上に **「–」（小さくする）** と **「⇆」（左右に移す）** を追加。小さくすると右下の小さな帯になり、中身（候補の選択など）はそのまま残る。「▢」かタイトルのダブルクリックで戻す。小さくした状態でもう一度ブックマークを押しても、作り直さずに元に戻す
- 候補と公式の画像は、押すと大きく表示（もう一度押すと閉じる）

## v24
- 取り込めなかったカードに**チェックボックス**（最初はオン）。オンのカードだけを依頼に入れる
- untap に候補（同じ番号に名前違いの登録が複数・区切りや 0 の違う番号の登録）があるカードは、**候補の画像・名前・登録番号・登録者**を公式の日本語版の画像と並べて表示。よく使われているものが先頭（「よく使われている」の印）
- 各候補に「これを使う」「名前をコピー」、ほかに「どれでもない」。選ぶとそのカードのチェックが外れ、「選んだカードで取り込み直す」で選んだ英語名に置き換えて取り込み直す（このブラウザにも保存）
- 選んだ結果は依頼文の「英語名の報告」欄で登録担当に届く（登録は不要、ws-names.json に足すだけ）。依頼するカードが無く報告だけのときは、ボタンが「英語名を報告する」になる
- 前回の「No cards where imported」の通知が残っていると、次の取り込みも失敗と誤判定することがあったのを修正

## v23
- **untap の表示変更に対応**：取り込めなかったカードの一覧が `.failed-imports`（見出しは小文字の「Cards failed import」、カード名から番号が消える）になり、失敗 0 件と誤判定していた。新しい一覧を読み、貼った行と突き合わせて番号を戻す（古い形も読む）
- 念のため untap のデッキ枚数（「25 Cards - 8 Unique」）と貼った枚数を比べ、一覧で説明できない不足があれば警告
- 入らなかったカードがあるときは「取り込みましたが、入らなかったカードがあります」と表示（「Save を押してください」だけにしない）。枚数チェックの表示は「元のデッキの枚数チェック」に
- デッキのページ：ヴァイスの「開く」は、開いたら「閉じる」に変わり、もう一度押すと閉じる

## v22
- ヴァイス：untap で「コピーしたデッキを取り込む」を押したとき、英語名データ（ws-names.json）に無いカードを **untap の内部検索に番号でまとめて問い合わせ**、番号が完全に一致した登録があれば untap の英語名に置き換えて取り込む（見つけた名前はこの untap のブラウザにも保存）
- 区切り・0 の有無が違う番号の登録は自動では使わず、「似た番号で登録あり」として表示し、依頼に `候補:` として書き添える
- 依頼文に `照合:` の行（済み：見つかった／候補あり／無し の数、まだ（デッキのページから）、できませんでした）
- デッキのページ：表示を「英語名データにある N / M 種類」に変更。依頼は「untap で取り込む前に頼む」（折りたたみ）に下げ、基本は untap で取り込んでから入らなかったものだけ頼む導線に
- 確認（2026-09-26、soogoo）：`card-search` に `{ccg:'wstcg', sets:[番号…]}` で番号から引ける（前方一致。複数まとめて可）。シャニマスの例で 16 種類中 5 種類が他の人により登録済みだった

## v21
- 「作成依頼」を **「照合・登録の依頼」** に改名（実際は untap にあるか調べて、あれば英語名を足すだけ、無ければ登録するため）。ボタンは「照合・登録を頼む」、説明は「untap にあるか調べて、無ければ登録してもらえます」
- 依頼文の1行目を `【untap 照合・登録の依頼】` に変更（受け取る側は旧 `【untap カード作成依頼】` も読むこと）

## v20
- ヴァイス：デッキのページで「開く」を押すと、**untap に登録済みか**（ws-names.json にあるか）を判定して「登録済み 12 / 16 種類」と表示。カードごとに 登録済み／未登録／要確認（名前が英字だけ）の印
- 未登録・要確認のカードは、untap を開かずに**その場で「作成依頼を送る」**
- **英語名の手入力欄をなくした**（HoC の訳は転載禁止のため）。以前ブラウザに保存した手入力の英語名も使わない。英語名は ws-names.json（登録担当が untap に登録した名前）だけ
- HoC へのリンクは「効果を読む（HoC）」として残す（読むだけ）。「英語名データを書き出す／読み込む」「カスタム登録用の一覧をコピー」はデッキのページから外した（untap 側の登録アシストには残る）

## v19
- ヴァイス：**名前が英字だけで英語名データに無いカード**（例「Shiny Days」）が取り込めたときは、「名前が同じ別のカードが入ったかもしれません」と警告し、作成依頼に「要確認」として入れる
- 確認したこと（2026-09-25、soogoo）：untap はカード名で探す。大文字小文字は無視。番号 `(set)` は同名カードの版を選ぶだけで、日本語名＋登録済みの番号では入らない。英語名＋違う番号でも名前が合えば入る。→ ヴァイスは英語名データ（ws-names.json）が必須

## v18
- untap で **1枚も一致しないとき**（「No cards where imported」と出て貼り付け画面が開いたままになる）も、全カードを取り込めなかったカードとして一覧にし、貼り付け画面を閉じて「作成依頼を送る」に進めるようにした
- 依頼の処理に「英語名の追加だけ」（untap に同じ番号のカードがもうあり、日本語名で貼ったため一致しなかっただけ）を追加。この場合は登録せず ws-names.json に英語名を足す

## v17
- **作成依頼を送る**：untap で取り込めなかったカードがあると「未登録のカードは、登録をお願いできます」と表示。「作成依頼を送る」で、依頼内容を入れた Google フォームが開く（長すぎるときはコピーして空のフォームを開く）。名前（任意）は次回も入る
- 依頼はフォームの回答シート → 1時間おきの定期処理（AI）→ 確認ボード → チャットで「OK」→ soogoo で登録、の流れで処理される

## v16
- **デッキを比べる**：カード名を日本語で表示（このデッキの日本語名、ワンピースは型番からも引く）。例「+4 インペルダウンの囚人 · Prisoner of Impel Down（46→50）」
- **ページの場所へ移動**：パネルのデッキ名（「↓ 場所へ」）を押すと、ページのそのデッキの見出しまでスクロールして光らせる（cardrush の記事・ヴァンガード公式・ヴァイス公式など、1ページに複数デッキがあるとき）

## v15
- **作成依頼**：untap で取り込めなかったカードを「作成依頼をコピー」でまとめてコピー（番号・日本語名・枚数・公式画像 URL・元のデッキ）
- **カード登録アシスト**：登録用データ（英語名・英語テキスト）を貼ると、Add Missing Card のフォームを自動入力（既存カードがあれば Add reprint、なければ新規。画像 URL・番号・Official print・Portrait まで。Add Card は人が押す）
- **登録後にもう一度取り込む**／untap 側でも ws-names.json を書き出せる
- ヴァイスの貼り付けに `(番号)` を付け、登録済みの英語名を番号で当てはめるようにした

## v14
- **untap 側の自動取り込み**：untap のデッキ画面でブックマークを押すと、コピーしたデッキを Paste Deck に自動で貼って取り込み、デッキ名も入れる（ゲームが違うときは止める）
- **履歴**：取り込んだデッキをデッキ名・成績・元の URL つきで最大100件、untap 側に保存（無料版の10デッキ制限の外で持っておける）。履歴から再取り込み・元ページを開ける
- コピー内容の末尾に `//c2u デッキ名 | 成績 | URL` の行を付けるようにした（untap には無視される）

## v9〜v13
- **ドン!!の指定（ワンピース）**：DON Deck に入れるドン!!を「名前 (版)」で変えられる（既定は通常の `Don!! Card (don-000)`、選んだものはブラウザに保存）
- **対戦用メモ**：英語名 → 日本語名の一覧と元ページの URL をコピー（untap の Play Notes 用）
- **デッキを比べる**：別のデッキのリストを貼ると、増えた・減ったカードを表示
- **ヴァイスの英語名の書き出し・読み込み**：`ws-names.json` に保存。GitHub に置くとどのサイト・どの PC でも自動で読み込む
- **枚数チェック**：ゲームごとの決まった枚数（メイン・EX・ライド・ドン!! など）と合っているかを表示
- **報告用コピー**：うまく動かないときに、ページの状況と動作記録をまとめてコピーできる
- **自動更新**：ブックマークは GitHub Pages の `d2u.js` を読み込む形に変更。本体を差し替えると全員に反映される（予備として全部入りのブックマークも残す）
- 毎週月曜の対応サイト自動点検（GitHub Actions）を追加

## 〜v8
- ONE PIECE：cardrush、TCG PORTAL
- 遊戯王：TCG PORTAL、DECK MAKER
- ポケモン：cardrush、TCG PORTAL、ポケカ公式のデッキ表示
- デュエマ：TCG PORTAL、DECK MAKER
- ヴァンガード：公式の入賞者デッキレシピ、DECK LOG
- ヴァイス：公式のデッキレシピ、DECK LOG（英語名は HoC を見て入力）
- 英語名が見つからないカードに、調べるページへのリンクを付ける
